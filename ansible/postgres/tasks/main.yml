---
# Assumes 'dnf' is the package manager (correct for Amazon Linux 2023)

- name: Install PostgreSQL 15 packages
  ansible.builtin.dnf:
    name:
    - postgresql15-server
    - postgresql15-contrib
    - python3-psycopg2 # Required for the postgresql_user/db modules
    state: present
  become: true

- name: Initialize PostgreSQL database cluster
  ansible.builtin.command:
    cmd: postgresql-setup --initdb
    creates: /var/lib/pgsql/data/postgresql.conf
  become: true

# ----------------- CRITICAL FIX ADDED HERE -----------------
- name: Set PostgreSQL to use MD5 password authentication for local connections
  ansible.builtin.lineinfile:
    path: /var/lib/pgsql/data/pg_hba.conf
    # The regex targets the default IPv4 local connection line (127.0.0.1/32)
    # and replaces 'ident' with 'md5'.
    regexp: '^host\s+all\s+all\s+127\.0\.0\.1\/32\s+ident$'
    line: 'host    all             all             127.0.0.1/32            md5'
    backup: yes
  become: true
  notify: Restart PostgreSQL
# -----------------------------------------------------------

- name: Enable and start PostgreSQL service
  ansible.builtin.service:
    name: postgresql
    state: started
    enabled: true
  become: true
# ... (rest of your tasks continue here)
# --- Database Configuration Tasks ---

- name: Create database user ({{ db_user }})
  ansible.builtin.postgresql_user:
    name: "{{ db_user }}"
    password: "{{ db_password }}"
    role_attr_flags: CREATEDB,LOGIN
  become: true
  become_user: postgres

- name: Create application database ({{ db_name }})
  ansible.builtin.postgresql_db:
    name: "{{ db_name }}"
    owner: "{{ db_user }}"
  become: true
  become_user: postgres

- name: Copy init.sql to server
  ansible.builtin.copy:
    src: ../db/init.sql
    dest: /tmp/init.sql
    mode: '0644'
  become: true

- name: Run init.sql to populate the database
  ansible.builtin.shell: "psql -d {{ db_name }} -f /tmp/init.sql"
  become: true
  become_user: postgres
  # Use 'shell' instead of 'command' for better execution context, although 'command' can work here.
